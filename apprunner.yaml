version: 1.0
runtime: python3
build:
  commands:
    pre-build:
      - python3.8.16
    build:      
      - pip install pipenv
      - pipenv install
      - pipenv install -r requirements.txt
run: 
  command: python app.py